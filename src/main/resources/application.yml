
spring:
  application:
    name: blackjack-api

  datasource:
    url: jdbc:mysql://localhost:3308/mydatabase
    username: ignasubirachs
    password: 1234A
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  data:
    mongodb:
      uri: mongodb://localhost:27017/blackjack-mongo
      database: blackjack-mongo


server:
  error:
    include-message: always

